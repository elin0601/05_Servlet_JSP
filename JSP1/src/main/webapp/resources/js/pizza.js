console.log("pizza.js 파일이 요청됨");

/* const plusBtn = document.querySelector(".row button:nth-of-type(2)");
const minusBtn = document.querySelector(".row button:nth-of-type(1)");

const input = document.querySelector(".row input");

plusBtn.addEventListener(click, function () {

    const amount = parseInt(input.amount);

    if (amount < parseInt(input.getAttribute('max'))) {

        input.amount = amount++;
    }
});

minusBtn.addEventListener(click, function () {

    const amount = parseInt(input.amount);

    if (amount > parseInt(input.getAttribute('min'))) {

        input.amount = amount--;
    }
});
 */

    
